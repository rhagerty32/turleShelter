<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Turtle Shelter - How'd you hear about us?</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<style>
    .volunteer-button-container {
    position: fixed; /* Fixes the button in the viewport */
    bottom: 20px; /* Positions it 20px from the bottom */
    right: 20px; /* Positions it 20px from the right */
    z-index: 1000; /* Ensures it's above other elements */
}

.volunteer-button {
    background-color: #808080; /* Matches the color of the "Submit" button */
    color: #fff; /* White text */
    padding: 8px 12px; /* Smaller padding for a smaller button */
    font-size: 14px; /* Adjust font size */
    border: none; /* Removes border */
    border-radius: 5px; /* Rounds the corners */
    cursor: pointer; /* Pointer cursor on hover */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Adds a slight shadow */
    transition: background-color 0.3s ease; /* Smooth transition for hover effect */
}

.volunteer-button:hover {
    background-color: #004d38; /* Darker green for hover */
}

</style>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const form = document.querySelector('form');
        const thankYouSection = document.getElementById('thank-you-section');
        const contactForm = document.getElementById('contact-form');

        form.addEventListener('submit', function (event) {
            contactForm.classList.add('hidden');
            thankYouSection.classList.remove('hidden');
        });
    });
</script>

<body class="bg-gray-50 font-sans">
    <!-- Main Content -->
    <div class="container mx-auto py-12 px-6 space-y-6">
        <!-- Thank You Section -->
        <section id="thank-you-section" class="bg-white p-8 rounded-lg shadow-md mt-5 hidden">
            <h2 class="text-2xl font-bold text-gray-800">Thank You for Letting us Know!</h2>
            
            <p class="mt-4 text-gray-700">
                We appreciate you helping us out.
            </p>
        </section>

        <!-- Contact Form -->
        <section id="contact-form" class="bg-white p-8 rounded-lg shadow-md">
            <h2 class="text-2xl font-bold text-gray-800">How'd you hear about us?</h2>
            <p class="mt-2 text-gray-600">Please select an option below:</p>
            <form action="/submitDiscoveryMethod" method="POST" class="mt-6 space-y-6">
                <div>
                    <label for="discoverymethod" class="block text-gray-700 font-medium">Discovery Method</label>
                    <select id="discoverymethod" name="discoverymethod" required
                        class="mt-2 w-full px-2 py-2 outline-teal-500 border rounded-lg shadow-sm focus:ring-teal-500 focus:border-teal-500">
                        <option value="" disabled selected>Select a method</option>
                        <option value="Family">Family</option>
                        <option value="Friend">Friend</option>
                        <option value="Past Event">Past Event</option>
                        <option value="Social Media">Social Media</option>
                        <option value="Other">Other</option>
                    </select>
                </div>
                <button type="submit"
                    class="w-full bg-teal-600 transition-all duration-300 text-white font-bold py-3 px-6 rounded-lg shadow hover:bg-teal-700">
                    Submit
                </button>
            </form>
        </section>
    </div>
    <div style="position: relative;">
    </form>
    <form action="/volunteerRequest" method="GET" style="text-align: center; margin-top: 10px;">
        <button type="submit" class="volunteer-button">Become a Volunteer</button>
    </form>
    </div>
    
    
    
    
</body>

</html>